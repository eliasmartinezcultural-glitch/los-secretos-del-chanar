/* V28.8 — deterministic route planner for autonomous actors */
(function(){'use strict';const N=window.ChanarV28Navigation={version:'28.8'};N.distance=function(a,b){return Math.hypot((b.x||0)-(a.x||0),(b.y||0)-(a.y||0))};N.direction=function(a,b){const dx=(b.x||0)-(a.x||0),dy=(b.y||0)-(a.y||0),d=Math.hypot(dx,dy)||1;return{x:dx/d,y:dy/d,distance:d}};N.plan=function(actor,destination,step){const d=N.direction(actor,destination),s=Math.min(step||1,d.distance);return{x:(actor.x||0)+d.x*s,y:(actor.y||0)+d.y*s,arrived:d.distance<=Math.max(step||1,4)}};N.route=function(actor,destinations){let out=[],cur={x:actor.x||0,y:actor.y||0};(destinations||[]).forEach((p,i)=>{out.push({index:i,x:p.x,y:p.y,fromDistance:N.distance(cur,p)});cur=p});return out};N.arrived=function(actor,target,radius){return N.distance(actor,target)<=(radius||18)};N.rule='Cada destino produce una ruta verificable; el movimiento autónomo debe terminar en un lugar y activar una acción.'})();
